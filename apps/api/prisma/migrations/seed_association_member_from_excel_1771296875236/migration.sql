-- ปิดการทำงานของ migration นี้โดยเจตนา (เดิมเป็นการนำเข้า AssociationMember 15 แถวจาก Excel)
--
-- เหตุผล: โฟลเดอร์นี้ไม่มี timestamp นำหน้า จึงเรียงเป็นลำดับสุดท้ายเสมอตามตัวอักษร และคำสั่ง
-- ข้างในเขียนลงคอลัมน์ `AssociationMember.memberId` ซึ่งถูกถอดออกไปแล้วตอนแยกโครงสร้างเป็น
-- AssociationMember (ตัวตนบุคคล) กับ Member (สมาชิกฌาปนกิจ) คำสั่งชุดเดิมจึงล้มด้วย error 1054
-- บนฐานข้อมูลใหม่ทุกครั้ง และไม่มีทางสำเร็จบนสคีมาปัจจุบันได้อีกเลย
--
-- ฐานข้อมูลที่ใช้งานอยู่บันทึกว่า migration นี้ applied ไปแล้ว ข้อมูล 15 แถวนั้นอยู่ในระบบแล้ว
-- ตัว SQL เดิมยังดูย้อนหลังได้จากประวัติ git ของไฟล์นี้
SELECT 1;
